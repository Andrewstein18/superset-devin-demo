// GitHub token — raises API rate limit from 60/hr to 5,000/hr.
// Create one at https://github.com/settings/tokens (no scopes needed for public repos).
window.GITHUB_TOKEN = "your-github-token-here";

// Devin API token — enables the "Active Sessions" card.
window.DEVIN_TOKEN = "your-devin-token-here";
